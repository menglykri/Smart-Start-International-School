{
  "name": "smart-start-school",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/styles.css -o ./styles.css --watch=always"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
